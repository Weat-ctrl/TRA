document.addEventListener('DOMContentLoaded', () => {
    const tunnel = document.getElementById('tunnel');
    const mandalaPattern = document.getElementById('mandalaPattern');
    
    // Adjust the rotation speed or other properties dynamically if needed
    mandalaPattern.style.animationDuration = '10s';
});
