document.addEventListener('DOMContentLoaded', () => {
    const mandalaPattern = document.getElementById('mandalaPattern');
    
    // Optional: Adjust the video properties dynamically if needed
    mandalaPattern.autoplay = true;
    mandalaPattern.loop = true;
    mandalaPattern.muted = true;
});
